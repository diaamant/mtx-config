# Mediamtx Configuration Editor

[![Python 3.12+](https://img.shields.io/badge/python-3.12+-blue.svg)](https://www.python.org/downloads/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

## 📌 Обзор

**Mediamtx Configuration Editor** — это интерактивное веб-приложение для управления конфигурацией сервера `mediamtx`. Приложение построено на базе фреймворка **NiceGUI** и предоставляет удобный интерфейс для настройки всех аспектов работы медиасервера.

### 🚀 Основные возможности
- **Централизованное управление** всеми настройками mediamtx
- **Визуальный редактор** с интуитивно понятным интерфейсом
- **Поддержка всех форматов** медиапотоков (RTSP, RTMP, HLS, WebRTC, SRT)
- **Гибкая настройка прав доступа** и аутентификации
- **Встроенная валидация** конфигурации
- **Автоматическое создание бэкапов** при сохранении
- **API** для интеграции с другими сервисами

## 🛠️ Особенности

### 📋 Основные функции
- **Интуитивный интерфейс** с вкладками для всех разделов конфигурации
- **Динамические формы** с автоматическим определением типов полей
- **Управление медиапотоками** с возможностью добавления, редактирования и удаления
- **Валидация конфигурации** перед сохранением
- **Автоматические бэкапы** с возможностью отката изменений
- **Модульная архитектура** для простоты поддержки и расширения

### 🎛️ Поддерживаемые настройки
- **Основные параметры** сервера
- **Аутентификация** и управление пользователями
- **Настройки RTSP/RTMP** серверов
- **Конфигурация HLS** и WebRTC потоков
- **Параметры SRT** потоков
- **Глобальные настройки** и значения по умолчанию

## 🚀 Быстрый старт

### Требования
- Python 3.12+
- uv (менеджер зависимостей)

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd mtx-config
   ```

2. **Установите uv (если еще не установлен):**
   ```bash
   curl -LsSf https://astral.sh/uv/install.sh | sh
   source $HOME/.cargo/env
   ```

3. **Установите зависимости:**
   ```bash
   uv venv
   source .venv/bin/activate
   uv pip install -r requirements.txt
   ```

4. **Запустите приложение:**
   ```bash
   python -m src.main
   ```

5. **Откройте в браузере:**
   ```
   http://localhost:8080
   ```

## 📦 Архитектура

### Компоненты системы
- **API слой** - обработка HTTP-запросов и маршрутизация
- **Сервисный слой** - бизнес-логика приложения
- **Слой данных** - работа с конфигурационными файлами
- **UI компоненты** - пользовательский интерфейс

### Используемые паттерны
- **Strategy** - для работы с разными форматами конфигураций
- **Singleton** - для управления настройками приложения
- **Factory** - для создания экземпляров клиентов
- **Observer** - для обновления UI при изменении данных

## 📚 Документация

Подробная документация доступна в директории `doc/`:
- `QUICK_START.md` - быстрое начало работы
- `CHANGELOG.md` - история изменений
- `TESTS.md` - руководство по тестированию
- `CONTRIBUTING.md` - руководство для разработчиков

## 🛠️ Технологический стек

- **Python 3.12+** - основной язык программирования
- **NiceGUI** - фреймворк для создания веб-интерфейсов
- **Pydantic** - валидация данных и моделирование схем
- **PyYAML** - работа с YAML файлами
- **uv** - менеджер зависимостей и виртуальных сред
- **Loguru** - логирование
- **Pytest** - модульное и интеграционное тестирование
- **Playwright** - end-to-end тестирование интерфейса

## 🧪 Тестирование

### Запуск тестов
```bash
# Модульные тесты
pytest tests/unit -v

# Интеграционные тесты
pytest tests/integration -v

# End-to-end тесты
pytest tests/e2e -v
```

### Покрытие кода
```bash
pytest --cov=src --cov-report=html
```

## 🤝 Вклад в проект

Приветствуются баг-репорты, запросы на доработку и предложения по улучшению. Пожалуйста, ознакомьтесь с [CONTRIBUTING.md](CONTRIBUTING.md) для получения дополнительной информации.

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для получения дополнительной информации.

## 📞 Контакты

По вопросам и предложениям обращайтесь в раздел Issues проекта.

## Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <repository-url>
    cd mtx-config
    ```

2.  **Установка uv:**

### An extremely fast Python package and project manager, written in Rust (https://github.com/astral-sh/uv)
    ```bash
    curl -LsSf https://astral.sh/uv/install.sh | sh
    source $HOME/.cargo/env
    ```
3.  **Создание и активация виртуальной среды:**
    ```bash
    uv venv
    source .venv/bin/activate
    ```

4.  **Установка зависимостей с помощью `uv`:**
    ```bash
    uv pip sync requirements.txt --link-mode=copy
    ```
    
5.  **Обновление зависимостей с помощью `uv`:**
    ```bash
    uv self update
    uv pip install --upgrade nicegui uvicorn
    ```

5.  **Настройка переменные окружения (опционально):**
    ```bash
    cp .env.template .env
    # Отредактируйте .env при необходимости
    ```

6.  **Запуск приложение:**
    ```bash
    cd /mnt/data/storeSoft/projPySample/mtx-config
    source .venv/bin/activate
    PYTHONPATH=/mnt/data/storeSoft/projPySample/mtx-config/src python -m src.main
    
    # python3 src/main.py
    ```

    Приложение будет доступно по адресу `http://localhost:8080`.

## 📖 Руководство пользователя

### Работа с потоками (Paths)

#### Добавление нового потока
1. Перейдите на вкладку **Paths**
2. Нажмите кнопку **"Добавить поток"**
3. Введите уникальное имя потока
4. Выберите тип: **Source** (RTSP/RTMP источник) или **RunOnDemand** (запуск по требованию)
5. Нажмите **"Добавить"**
6. Настройте параметры потока в появившейся панели

#### Поиск и фильтрация потоков
- Используйте поле поиска для фильтрации по имени
- Выберите тип в выпадающем списке для фильтрации по типу потока
- Статистика отображает общее количество, Source и RunOnDemand потоков

#### Клонирование потока
1. Нажмите кнопку **копирования** (📋) рядом с нужным потоком
2. Введите новое имя для копии
3. Нажмите **"Клонировать"**
4. Измените параметры клонированного потока при необходимости

#### Удаление потока
1. Нажмите кнопку **удаления** (🗑️) рядом с потоком
2. Подтвердите удаление в диалоговом окне

### Валидация конфигурации

Нажмите кнопку **"Валидация"** в заголовке для проверки:
- Корректность URL источников (rtsp://, rtmp://, http://)
- Правильность формата timeout (10s, 5m, 1h)
- Заполненность обязательных полей
- Другие параметры конфигурации

Ошибки и предупреждения отображаются в виде уведомлений.

### Предпросмотр YAML

1. Нажмите кнопку **"Предпросмотр"** или перейдите на вкладку **Preview**
2. Просмотрите итоговую конфигурацию перед сохранением
3. Используйте кнопку **"Обновить"** для актуализации предпросмотра

### Сохранение

1. Нажмите кнопку **"Сохранить"** (или `Ctrl+S`)
2. Все данные сохраняются в JSON файлы
3. Автоматически создается бэкап `mediamtx01.yml.bak`
4. Генерируется итоговый файл `work/mediamtx01.yml`

### Подсказки

Наведите курсор на названия параметров для просмотра подсказок с описанием и примерами значений.

## Как это работает

1.  **Источник данных:** Приложение работает с набором `.json` файлов в каталоге `work/json/`. Эти файлы представляют собой разделенные по категориям части конфигурации `mediamtx`.
2.  **Валидация:** При загрузке и изменении данных используются Pydantic модели для проверки корректности
3.  **Интерфейс:** `main.py` запускает веб-сервер и строит интерфейс, используя компоненты из `src/ui_components`
4.  **Редактирование:** Пользователь вносит изменения в поля ввода. Все изменения валидируются и сохраняются в словаре `data` в памяти
5.  **Сохранение:** При нажатии кнопки "Сохранить":
    -   Данные из словаря `data` записываются обратно в `.json` файлы
    -   Создается бэкап `mediamtx01.yml.bak`
    -   Все `.json` файлы (которые отмечены как "включенные") собираются в один словарь
    -   Финальный словарь конвертируется в YAML и сохраняется в `work/mediamtx01.yml`

### Структура проекта:
```
src/
├── api/                  # API endpoints
├── clients/              # Клиенты для работы с конфигурацией
├── core/                 # Ядро приложения
├── models/               # Pydantic модели
├── service/              # Бизнес-логика
├── ui_components/        # Компоненты пользовательского интерфейса
├── utils/                # Вспомогательные утилиты
└── work/                                  # Рабочие файлы
    ├── json/                              # JSON файлы конфигурации
    │   ├── auth.json
    │   ├── paths.json                     
    │   ├── values_app.json
    │   └── ... (остальные json файлы)
    ├── mediamtx01.yml                     # Финальный YAML файл для mediamtx
    └── mediamtx01.yml.bak                 # Бэкап предыдущей версии
```


## 🧪 Тестирование

Проект включает комплексный набор тестов для проверки всей функциональности.

### Типы тестов

#### Unit тесты
Проверяют отдельные модули и функции:
- **Модели данных** (`test_models.py`) - валидация Pydantic моделей
- **Конфигурационные клиенты** (`test_config_clients.py`) - JSONClient и YAMLClient
- **Утилиты** (`test_json_utils.py`) - функции загрузки/сохранения JSON и YAML через клиентов
- **Настройки** - singleton паттерн и конфигурация

#### Playwright E2E тесты
Проверяют пользовательский интерфейс:
- **Структура интерфейса** - наличие элементов и навигация
- **Взаимодействие** - фильтры, поиск, кнопки
- **Интеграция** - полная работа приложения

### Запуск тестов

#### Unit тесты (с покрытием кода)
```bash
source .venv/bin/activate
python -m pytest tests/test_config_clients.py tests/test_json_utils.py tests/check_models.py -v
```

#### Playwright E2E тесты
```bash
source .venv/bin/activate
./run_tests.sh
```

Или вручную:
```bash
source .venv/bin/activate
# Запустить приложение в фоне
PYTHONPATH=/mnt/data/storeSoft/projPySample/mtx-config/src python -m src.main &
APP_PID=$!

# Запустить тесты
python -m pytest tests/test_filters.py -v -m playwright

# Остановить приложение
kill $APP_PID
```

#### Все тесты вместе
```bash
source .venv/bin/activate
python -m pytest tests/ -v
```

### Структура тестов

- `tests/test_models.py` - Тесты для Pydantic моделей валидации
- `tests/test_config_clients.py` - Тесты для конфигурационных клиентов
- `tests/test_json_utils.py` - Тесты для JSON/YAML утилит через клиентов
- `tests/test_filters.py` - Playwright тесты для интерфейса

### Конфигурация тестирования

- `pytest.ini` - основная конфигурация pytest
- `pytest-unit.ini` - конфигурация для unit тестов с coverage
- `pytest-playwright.ini` - конфигурация для Playwright тестов
- `run_tests.sh` - автоматический запуск Playwright тестов

### Покрытие кода

После запуска unit тестов с опцией `--cov-report=html`, откройте `htmlcov/index.html` в браузере для просмотра детального отчета о покрытии кода.

## 🔧 Разработка

### Линтинг и форматирование

```bash
# Установить pre-commit hooks (рекомендуется)
pip install pre-commit
pre-commit install

# Запустить линтер вручную
ruff check src/

# Автоматическое исправление
ruff check --fix src/
```

### Добавление новых функций

1. Создайте feature branch
2. Реализуйте функционал с type hints
3. Добавьте тесты для новой функциональности
4. Убедитесь что все тесты проходят
5. Обновите документацию

## 🐛 Отладка

Приложение использует стандартный модуль `logging` Python. Логи выводятся в консоль.

### Уровни логирования

Настройте уровень через переменную окружения:

```bash
export MTX_LOG_LEVEL=DEBUG  # DEBUG, INFO, WARNING, ERROR
python3 src/main.py
```

### Частые проблемы

**Проблема:** Порт 8080 уже занят

**Решение:** Измените порт через `.env`:
```bash
MTX_APP_PORT=8081
```

**Проблема:** Ошибка валидации при загрузке

**Решение:** Проверьте JSON файлы на корректность формата. Логи укажут конкретный файл и ошибку.

**Проблема:** Изменения не сохраняются

**Решение:** Проверьте права доступа к директории `work/`. Логи покажут ошибки записи.

## 📝 Changelog

История изменений доступна в файле [CHANGELOG.md](CHANGELOG.md).

### Последние изменения (кратко)

**v0.3.0 (2025-10-22)**
- 🏗️ **Архитектурный рефакторинг:** Переход на паттерн Strategy с использованием конфигурационных клиентов
- 🧪 **Расширенное тестирование:** Новые тесты для клиентов и обновленные существующие тесты
- 📚 **Обновленная документация:** Полное описание архитектуры и тестирования
- 🐛 **Исправления:** Проблемы с импортами, мокингом и логикой сохранения конфигурации

## 🤝 Вклад в проект

Приветствуются любые улучшения! См. `doc/task/task01.md` для списка предложенных улучшений.

## 📄 Лицензия

MIT

## 🔗 Ссылки

- [mediamtx GitHub](https://github.com/bluenviron/mediamtx)
- [NiceGUI Documentation](https://nicegui.io/)
- [Pydantic Documentation](https://docs.pydantic.dev/)
