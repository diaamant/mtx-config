[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
    --browser=chromium
    --headed=false
markers =
    playwright: Playwright end-to-end tests
